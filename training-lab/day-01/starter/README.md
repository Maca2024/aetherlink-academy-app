# Day 1 starter

Import this folder in the assignment card with `assignment.starterPath: day-01/starter`.

The starter calls the real local weather endpoint. It does not call a public weather provider.
