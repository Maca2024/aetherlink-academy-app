# Day 4. Fraud signals

Analyze the generated fictional `transactions.csv`. The generator writes a fixed dataset of six transactions, including three suspicious cases. Each result must use the format `Risk / Reason / Recommended action`.

The starter path is `day-04/starter`. Re-run `node data/generate.mjs` to regenerate the same file. `tests/expected.json` records the expected suspicious IDs and hash.
